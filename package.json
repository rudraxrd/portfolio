{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Rudra — Portfolio",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8000",
    "preview": "vite preview --host 0.0.0.0 --port 8000",
    "serve": "python3 -m http.server 8000 --bind 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
